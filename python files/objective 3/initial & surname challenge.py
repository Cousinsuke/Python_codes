Fname = input("what is your forename?")#question forename

Sname = input("what is your surname?")#question surname

init = (Fname[0:1])#gets the first letter only
init = init.upper()#capitalise the initial

Sname  = Sname.upper()#capitalises the whole name
print (init, Sname)
